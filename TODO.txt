- migrate everything to aws services: cloudformation, bedrock, s3, dynamodb, api gateway, cloudfront, elastic load balancing, cloudwatch, config, cognito, shield
- populate repo variables, VITE_API_BASE_URL, VITE_COGNITO_DOMAIN, VITE_COGNITO_CLIENT_ID, VITE_COGNITO_REDIRECT_URI.
- check with claude if we're being realistic about everything.
- remodel skeleton frontend for everything that we're going to need for prop 1.
